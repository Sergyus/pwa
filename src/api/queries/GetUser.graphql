query GetUsers($params: PaginationInput!) {
  users(pagination: $params) {
    data {
      id
      name
      username
      email
      phone
      website
    }
    count
    currentPage
    totalPages
  }
}
